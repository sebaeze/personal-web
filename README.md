## Pagina personal para web site de Sebastian Andreoletti

git clone
npm install
npm install -D parcel-bundler babel-preset-react babel-preset-env

### Test
parcel ./src/index.html

### Build && run
npm run build
npm start